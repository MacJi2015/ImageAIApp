# 复制为 .env 并填写实际值，.env 已被 git 忽略，不会上传
#
# API_BASE_URL=https://api.petsai.net/facial
#
# --- Google（已正常时可沿用 Firebase / 现有配置）---
# GOOGLE_WEB_CLIENT_ID=xxx.apps.googleusercontent.com
#
# --- Facebook（Meta）：除环境变量外，必须改 iOS Info.plist / Android strings.xml 里的占位符，见 docs/THIRD_PARTY_LOGIN.md ---
# 原生侧：FacebookAppID、FacebookClientToken、fb<APP_ID> URL Scheme；Firebase 控制台需启用 Facebook 登录并填 App Secret
#
# --- X（Twitter）：后端需实现授权 URL + PKCE 兑换；X 开发者平台 Callback 须与下一致 ---
# X_REDIRECT_URI=imageai://auth/x
# 若 GET /auth/social/authorize-url?provider=x 不可用，可填完整授权页兜底：
# X_AUTHORIZE_URL=https://...
#
# --- TikTok：OpenSDK 用 HTTPS 回调；须与 TikTok 开放平台、后端 sdk-exchange 一致 ---
# TIKTOK_OPENSDK_REDIRECT_URI=https://你的域名/.../tiktok/callback
#
# 说明：Instagram / X / TikTok 的 WebView 流程还依赖后端
# GET {baseURL}/auth/social/authorize-url?provider=instagram|x|tiktok
# 授权结束需重定向到 imageai://auth/instagram|x|tiktok?token=... 或 X PKCE：imageai://auth/x?code=...&state=...
