# 复制为 .env 并填写实际值，.env 已被 git 忽略，不会上传
# API_BASE_URL=https://api.example.com
# X OAuth 2.0 PKCE 回调（需与 X 开发者平台及后端一致，默认 imageai://auth/x）
# X_REDIRECT_URI=imageai://auth/x
# X Firebase 登录页：若后端用 Firebase 做 Twitter 登录且未提供 authorize-url 接口，填后端给的登录页完整 URL（授权完成后需重定向到 imageai://auth/x?token=xxx）
# X_AUTHORIZE_URL=https://your-api.com/auth/x or https://your-project.firebaseapp.com/__/auth/...
